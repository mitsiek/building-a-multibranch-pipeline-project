pipeline {
    agent any

    environment {
        
                                      BUILD_USER_EMAIL=${BUILD_USER_EMAIL}
                                  }

    stages {
        stage('Build') {
            steps {
                sh 'printenv'
            }
        }
    }
}
